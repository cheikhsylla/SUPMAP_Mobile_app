name: mobile_app
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  auth0_flutter: ^1.11.0
  flutter_appauth: ^6.0.0
  provider: ^6.0.5
  http: ^0.13.6
  google_maps_flutter: ^2.5.3
  geolocator: ^11.0.0
  google_place: ^0.4.7
  permission_handler: ^11.3.1
  url_launcher: ^6.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
    - assets/google_icon.png
